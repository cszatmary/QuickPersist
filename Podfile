# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
platform :osx, '10.9'

target 'QuickPersist-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for QuickPersist-iOS
  pod 'RealmSwift', '~> 3.0.0-rc'

  target 'QuickPersist-iOS Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'QuickPersist-macOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for QuickPersist-macOS
  pod 'RealmSwift', '~> 3.0.0-rc'

  target 'QuickPersist-macOS Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'QuickPersist-tvOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for QuickPersist-tvOS
  pod 'RealmSwift', '~> 3.0.0-rc'

  target 'QuickPersist-tvOS Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'QuickPersist-watchOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for QuickPersist-watchOS
  pod 'RealmSwift', '~> 3.0.0-rc'

end
